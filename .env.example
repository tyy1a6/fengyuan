# 复制为 .env 后修改；docker-compose 会自动读取同目录下的 .env

# 容器内监听端口（配合 docker-compose 的 ports 映射）
PORT=3000

# 运行环境
NODE_ENV=production

# 允许跨域来源：* 表示不限制；若用自有域名可改为 https://你的域名
CORS_ORIGIN=*

# 后台管理（/admin）登录密码
ADMIN_PASSWORD=fengyuan@2026

# 内容数据持久化目录（务必挂载为 volume，否则容器重建会丢失后台编辑）
DATA_DIR=/app/backend/data
