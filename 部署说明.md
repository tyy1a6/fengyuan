# 凤院村门户 · 自托管部署说明

本项目为前后端一体应用：后端 Express 同时提供 API（`/api`）并托管前端构建产物，
**只需一个端口**即可对外服务。以下说明帮助你把项目部署到自己的服务器，彻底脱离云端。

## 一、前置要求

- 一台能联网的服务器（Linux / macOS / Windows 均可）
- 已安装 **Docker** 与 **Docker Compose**
  - 验证：`docker -v` 与 `docker compose version`

## 二、准备文件

把整个项目目录拷贝到服务器（通过 `git clone` 或导出代码后上传），确保包含：
`Dockerfile`、`docker-compose.yml`、`.dockerignore`、`backend/`、`frontend/`。

可选：复制环境变量模板并按需修改

```bash
cp .env.example .env
# 编辑 .env：至少把 ADMIN_PASSWORD 改成你自己的密码
```

## 三、一键启动

在项目根目录执行：

```bash
docker compose up -d --build
```

- 首次会自动构建前端、安装后端依赖，约 1~3 分钟
- 启动后访问：`http://服务器IP:3000`
- 后台管理：`http://服务器IP:3000/admin`（密码见 `.env` 的 `ADMIN_PASSWORD`，默认 `fengyuan@2024`）

查看日志：`docker compose logs -f`
停止服务：`docker compose down`
重新构建：`docker compose up -d --build`

## 四、数据持久化（重要）

后台 `/admin` 里编辑的站点配置、文档会写入容器内的
`/app/backend/data`，已通过 `docker-compose.yml` 挂载到宿主机的 `./data` 目录。
**只要 `./data` 目录保留，容器重建、升级都不会丢失内容。**

> 注意：前端图片/视频等媒体在构建时已打进静态文件，不在 `./data` 中，
> 如需替换媒体，请改 `frontend/public/media/` 后重新 `docker compose up -d --build`。

## 五、用自有域名 + HTTPS（推荐）

在服务器前置一个 Nginx 反向代理即可。示例（Nginx）：

```nginx
server {
    listen 80;
    server_name 你的域名;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

随后用 certbot 等工具申请免费 HTTPS 证书。证书生效后，可把
`docker-compose.yml` 中的 `CORS_ORIGIN` 改为你的域名 `https://你的域名`。

## 六、不用 Docker 的纯 Node 方式

若不想用 Docker，也可直接在服务器上运行：

```bash
# 1) 构建前端
cd frontend
npm install -g pnpm
pnpm install
pnpm build          # 产物在 frontend/dist

# 2) 启动后端（会托管 frontend/dist 并提供 API）
cd ../backend
npm install
export NODE_ENV=production
export PORT=3000
export ADMIN_PASSWORD=你的密码
export DATA_DIR=$(pwd)/data
mkdir -p data
./node_modules/.bin/tsx src/index.ts
```

建议用 `pm2` 或 `systemd` 守护进程，保证后台常驻。

## 七、环境变量一览

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `PORT` | 容器内监听端口 | `3000` |
| `NODE_ENV` | 运行环境 | `production` |
| `CORS_ORIGIN` | 跨域允许来源，`*` 为不限制 | `*` |
| `ADMIN_PASSWORD` | `/admin` 后台登录密码 | `fengyuan@2024` |
| `DATA_DIR` | 内容数据持久化目录 | `backend/data` |

## 八、常见问题

- **打不开页面**：先 `docker compose ps` 看容器是否 `Up`；再 `docker compose logs` 看报错。
- **后台改了内容重启后没了**：确认 `./data` 目录已挂载且未被误删。
- **端口冲突**：改 `docker-compose.yml` 的 `ports` 映射，如 `"8080:3000"`。
